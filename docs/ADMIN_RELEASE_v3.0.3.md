# 统一管理后台 v3.0.3 发布录入信息

后台地址：`https://admin.xibao-zg.top`
产品编码：`publisher.douyin`

## 产品版本

| 字段 | 填写值 |
| --- | --- |
| 版本号 | `3.0.3` |
| 版本代码 | `30003` |
| 渠道 | `stable` |
| 状态 | 发布 |
| 最低支持版本 | `2.3.0` |
| 更新策略 | 可选更新 / 不强制 |
| 灰度比例 | `100%` |
| 下载地址 | `https://github.com/Hugh112/douyinshangchuan/releases/download/v3.0.3/DouyinPublisher_Update_v3.0.3.zip` |
| SHA256 | `d708efc6787c3c65dc8eb75373780e48dae117010722ef036ea3f22bdb3fc5b2` |
| 文件字节数 | `89868738` |

版本说明：

```text
1. 修复任务控制区按钮被右侧裁切，重置发布进度和清空日志现在完整显示。
2. 修复选择定时发布后目标日期时间未实际写入；使用真实键盘输入完整时间并回读校验。
3. 发布设置已在页面底部可见时跳过下滑、鼠标滚轮和等待，减少无效操作。
4. DOM 下滑到位后不再追加滚轮；网页内部容器仍未到位时保留真实滚轮兜底。
```

## 更新公告

公告标题：`抖音智能发布中心 v3.0.3 已发布`

公告正文：

```text
v3.0.3 为可选更新。本次修复定时发布时间未正确写入、任务控制按钮被遮挡，并优化发布页已到底部时的重复下滑和等待。原有多浏览器队列、账号轮换、城市图片、独立 Excel/排期、失败重试和进度恢复继续保留。账号密码请联系各组组长获取。
```

公告范围：`publisher.douyin`
公告状态：发布

## 后台操作顺序

1. 打开对应产品的“版本与公告”，创建版本草稿。
2. 按上表填写所有字段，确认 SHA256 为 64 位小写十六进制，文件大小使用字节值。
3. 发布版本；不要启用强制更新。
4. 创建并发布上述产品公告。
5. 使用已授权的 v3.0.2 客户端点击“检查更新”，确认显示 `3.0.2 → 3.0.3`。

如后台已有更高版本代码，`30003` 会被拒绝；此时使用高于当前最大值的新代码，并同时记录到发布审计。版本号、下载地址、SHA256 和包大小不得改变。
