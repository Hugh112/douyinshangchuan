# 统一管理后台 v3.0.4 发布录入信息

后台地址：`https://admin.xibao-zg.top`
产品编码：`publisher.douyin`

| 字段 | 填写值 |
| --- | --- |
| 版本号 | `3.0.4` |
| 版本代码 | `30004` |
| 渠道 | `stable` |
| 最低支持版本 | `2.3.0` |
| 更新策略 | 可选更新 / 不强制 |
| 灰度比例 | `100%` |
| 下载地址 | `https://github.com/Hugh112/douyinshangchuan/releases/download/v3.0.4/DouyinPublisher_Update_v3.0.4.zip` |
| SHA256 | `be8a3b2c0e745ff826bd06876f1194f06bbf64e51a762144acf8a946005f4b21` |
| 文件字节数 | `89869975` |

版本说明：

```text
1. 修复新版抖音已选中定时发布，却被客户端误判为点击失败的问题。
2. 等待发布时间框出现后，严格执行清空旧值、写入账号排期目标时间和回读确认。
3. 受控日期组件使用 v3.0.1 已验证的 DOM 输入方式，并保留键盘和日期面板兜底。
4. 点击底部发布前再次回读定时时间；值不一致时禁止提交。
```

公告标题：`抖音智能发布中心 v3.0.4 定时时间修复`

公告正文：

```text
v3.0.4 为可选更新。本次修复抖音新版定时发布控件已选中却被误判失败，以及定时时间未实际写入的问题。程序会等待时间框出现，清空旧值，写入账号排期时间并两次回读确认；时间不一致时不会点击发布。原有多浏览器、轮换发布、独立进度及其他功能继续保留。
```

如尚未在后台发布 v3.0.3，可直接只发布 v3.0.4。发布后使用 v3.0.3 客户端点击“检查更新”，应显示 `3.0.3 → 3.0.4`。
